// @ts-ignore - SvelteKit types will be generated
export const load = async ({ data }: any) => {
  // Basic layout load - authentication will be handled by individual route layouts
  return {
    ...data
  }
}
